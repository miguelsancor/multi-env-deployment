apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: dev  # Cambia 'dev' por 'prod' si este ConfigMap es para producción
data:
  environment: "development"  # Cambia 'development' por 'production'
  log_level: "info"          # Usa un nivel de log apropiado para producción
